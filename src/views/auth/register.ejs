<!DOCTYPE html>
<html lang="fr" class="h-full">
<head>
  <meta charset="UTF-8">
  <link rel="icon" href="/img/voilavoila-favicon.webp" type="image/webp">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Créer un compte — Voilà Voilà Tools</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css">
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.1/dist/cdn.min.js"></script>
</head>
<body class="h-full flex">

  <!-- ── Panneau gauche : bleu ── -->
  <div class="hidden lg:flex lg:w-[55%] vv-sidebar flex-col justify-between p-12">
    <div>
      <img src="/img/voilavoila-logo-blanc.webp" alt="Voilà Voilà Tools" class="h-8 object-contain">
    </div>
    <div>
      <h1 class="text-white font-display text-4xl font-bold leading-tight mb-4">
        Rejoignez la plateforme<br>des créatifs.
      </h1>
      <p class="text-white/60 text-base leading-relaxed max-w-sm">
        Créez votre compte gratuitement et commencez à générer des outils de communication professionnels en quelques minutes.
      </p>
    </div>
    <div class="flex items-center gap-6">
      <div class="text-center">
        <p class="text-white text-2xl font-bold">QR</p>
        <p class="text-white/50 text-xs mt-0.5">Codes</p>
      </div>
      <div class="w-px h-8 bg-white/20"></div>
      <div class="text-center">
        <p class="text-white text-2xl font-bold">✉</p>
        <p class="text-white/50 text-xs mt-0.5">Signatures</p>
      </div>
      <div class="w-px h-8 bg-white/20"></div>
      <div class="text-center">
        <p class="text-white text-2xl font-bold">☑</p>
        <p class="text-white/50 text-xs mt-0.5">VCards</p>
      </div>
    </div>
  </div>

  <!-- ── Panneau droit : formulaire ── -->
  <div class="flex-1 flex flex-col items-center justify-center px-6 py-12 bg-white overflow-y-auto">

    <!-- Logo mobile -->
    <div class="lg:hidden mb-8 text-center">
      <img src="/img/Logo_Voila Voila_RVB.png" alt="Voilà Voilà Tools" class="h-10 object-contain mx-auto">
    </div>

    <div class="w-full max-w-sm">
      <div class="mb-8">
        <h2 class="text-2xl font-bold text-gray-900">Créer un compte</h2>
        <p class="text-sm text-gray-500 mt-1">Votre espace de travail en quelques secondes</p>
      </div>

      <%- include('../partials/flash') %>

      <% if (errors && errors.length > 0) { %>
      <div class="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg">
        <% errors.forEach(e => { %><p class="text-sm text-red-700"><%= e.msg %></p><% }) %>
      </div>
      <% } %>

      <form method="POST" action="/auth/register" class="space-y-4">
        <div>
          <label class="form-label" for="name">Nom complet</label>
          <input type="text" id="name" name="name" required class="form-input"
                 placeholder="Prénom Nom" value="<%= old?.name || '' %>">
        </div>
        <div>
          <label class="form-label" for="email">Email</label>
          <input type="email" id="email" name="email" required class="form-input"
                 placeholder="vous@exemple.fr" value="<%= old?.email || '' %>">
        </div>
        <div>
          <label class="form-label" for="password">Mot de passe</label>
          <input type="password" id="password" name="password" required class="form-input"
                 placeholder="8 caractères minimum" minlength="8">
        </div>
        <div>
          <label class="form-label" for="password_confirm">Confirmer le mot de passe</label>
          <input type="password" id="password_confirm" name="password_confirm" required class="form-input" placeholder="••••••••">
        </div>
        <button type="submit" class="btn-primary w-full justify-center py-3 mt-2">
          Créer mon compte
        </button>
      </form>

      <p class="text-center text-sm text-gray-500 mt-6">
        Déjà un compte ?
        <a href="/auth/login" class="text-brand-600 hover:text-brand-700 font-semibold">Se connecter</a>
      </p>
    </div>
  </div>

</body>
</html>
